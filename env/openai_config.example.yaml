# OpenAI API 配置模板
# 复制此文件为 openai_config.yaml 并填入你的真实配置
# 注意：openai_config.yaml 已在 .gitignore 中排除，不会被提交到 Git

model: "gpt-4o"           # 模型 ID
base_url: ""               # API 地址（留空使用 OpenAI 官方地址）
api_key: ""                # API 密钥